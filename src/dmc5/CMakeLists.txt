PROJECT(dmc5)

ADD_LIBRARY(${PROJECT_NAME} SHARED main.cpp util/ini.h )
SET_TARGET_PROPERTIES(${PROJECT_NAME} PROPERTIES FOLDER ${PROJECT_NAME})
set_target_properties(${PROJECT_NAME} PROPERTIES OUTPUT_NAME MFReadWrite)
TARGET_LINK_LIBRARIES(${PROJECT_NAME} hacklib)